
<center>
<form action="">
  Enter The Given Passphrase to Start Voting
  <br>
  <input type="text" name="fname">
  <br>
  <input type="submit" value="Submit">`
</form>
</center>


<br><br><br><br>
<% if signed_in? %>
  <%= current_user.email %>
  <%= button_to 'Control Panel', admin_path, method: :get %>
  <%= button_to 'Sign out', sign_out_path, method: :delete %>
<% else %>
  <%= link_to 'Admin Sign In', sign_in_path %>
<% end %>

<div id="flash">
  <% flash.each do |key, value| %>
    <div class="flash <%= key %>"><%= value %></div>
  <% end %>
</div>